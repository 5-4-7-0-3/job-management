export enum EmployerStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}

export enum JobStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  ARCHIVED = 'archived'
}

export enum WorkerHistoryEvent {
  HIRED = 'hired',
  FIRED = 'fired',
}

export enum WorkerOperation {
  HIRE = 'hire',
  FIRE = 'fire',
}